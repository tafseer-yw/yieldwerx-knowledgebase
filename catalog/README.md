# Source catalog

`sources.json` records the identity, authority, status, path, size, and SHA-256 hash
of every source document stored in this repository.

Run `npm run catalog` after adding, replacing, or removing a source.

The duplicate First Edition PDF found in the old support-material folder is not
included. The canonical First Edition files are stored once under
`sources/archive/first-edition/`.
